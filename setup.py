from setuptools import setup, find_packages

setup(
    name='deep-text-recognition-benchmark',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List dependencies here, if known
    ],
)